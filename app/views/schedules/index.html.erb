<div id="dg">
  <div class="container">
    <div class="row centered">
      <h4>Hyderabad branch Schedules</h4>
      <br>
      <% @schedules.each do |schedule| %>
      <div class="col-lg-4">
        <div class="pricing-option">
          <div class="pricing-top">
            <span class="special-label"><small><%= schedule.try(:status) %></small></span>
            <span> <%= schedule.course.name %> </span>
            <br>   <br>
            <span><%= schedule.try(:start_date) %> &nbsp;&nbsp;<small> Duration :<%= schedule.try(:duration) %> Days</small></span>
            <br>   <br>
            <span><button class="btn_register" data-toggle="modal" data-target="#register_course" href="#myModalLabel" data-course="<%= schedule.course.name %>" data-scheduleid="<%= schedule.id %>"><i class="fa fa-hand-o-right"> Register</i></button>&nbsp;&nbsp;&nbsp; <a href="target=&quot;_blank&quot;"><i class="fa fa-book">&nbsp;Course contents</i> </a> </span>
            </div>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>
  <%= render :partial => "/courses/register", locals: {register: @register}  %>
  <!-- MODAL FOR CONTACT -->
  <%= render :partial => "/courses/contact_model"  %>


  <script>
  $(document).ready(function(){
    $(".btn_register").click(function(){
      console.log($(this));
      $('#register_schedule_id').val($(this).data('scheduleid'));
      $('#register_course_name').val($(this).data('course'));
    });

    $("#my-form").submit(function(){
      alert('afsdsf');
    });
  });
</script>
